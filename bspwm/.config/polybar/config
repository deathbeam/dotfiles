; vim:ft=dosini
include-file = $BASE16_POLYBAR_PATH

[bar/main]
monitor = ${env:MONITOR:}
wm-restack = bspwm
override-redirect = false
width = 10iz
height = ${env:POLYBAR_BAR_HEIGHT:40}
dpi-x = ${env:DPI:-1}
dpi-y = ${env:DPI:-1}
offset-x = 0
offset-y = 0
bottom = false
border-bottom-size = 0
spacing = 0
underline-size = 0
overline-size = 0
padding-left = 0
padding-right = 2
module-margin-left = 0
module-margin-right = 2

background = ${colors.base01}
foreground = ${colors.base05}
font-0 = monospace:size=12:antialias=false:autohint=false;2
font-1 = Terminess Nerd Font:size=12:antialias=true:autohint=true;2

modules-left = bspwm title
modules-right = tray wifi wired cpu memory nvidia-smi volume backlight battery date

[module/bspwm]
type = internal/bspwm
format = <label-state><label-mode>

label-dimmed-foreground = ${colors.base04}

label-focused = %name%
label-focused-background = ${colors.base0D}
label-focused-foreground = ${colors.base00}
label-focused-padding = 1

label-empty = %name%
label-empty-background = ${colors.base00}
label-empty-foreground = ${colors.base04}
label-empty-padding = 1

label-urgent = %name%
label-urgent-background = ${colors.base08}
label-urgent-foreground = ${colors.base00}
label-urgent-padding = 1

label-occupied = %name%
label-occupied-padding = 1

label-monocle = m
label-monocle-background = ${colors.base0C}
label-monocle-foreground = ${colors.base00}
label-monocle-padding = 1

label-tiled = t
label-tiled-background = ${colors.base0B}
label-tiled-foreground = ${colors.base00}
label-tiled-padding = 1

label-fullscreen = F
label-fullscreen-background = ${colors.base0B}
label-fullscreen-foreground = ${colors.base00}
label-fullscreen-padding = 1

label-floating = f
label-floating-background = ${colors.base0F}
label-floating-foreground = ${colors.base00}
label-floating-padding = 1

label-pseudotiled = P
label-pseudotiled-background = ${colors.base0B}
label-pseudotiled-foreground = ${colors.base00}
label-pseudotiled-padding = 1

label-locked = l
label-locked-background = ${colors.base0B}
label-locked-foreground = ${colors.base00}
label-locked-padding = 1

label-sticky = s
label-sticky-background = ${colors.base0B}
label-sticky-foreground = ${colors.base00}
label-sticky-padding = 1

label-private = p
label-private-background = ${colors.base0B}
label-private-foreground = ${colors.base00}
label-private-padding = 1

[module/wifi]
type = internal/network
interface = wlan0
interval = 3.0
format-connected = <label-connected>
label-connected =   %essid%

[module/wired]
type = internal/network
interface = eth0
interval = 3.0
label-connected =  %{T3}%local_ip%%{T-}

[module/date]
type = internal/date
date =   %Y-%m-%d   %H:%M
interval = 5

[module/volume]
type = internal/alsa
format-volume = <ramp-volume>  <label-volume>
label-volume = %percentage%
label-muted = 
ramp-volume-0 = 
ramp-volume-1 = 
ramp-headphones-0 = 

[module/battery]
type = internal/battery
battery = BAT0
full-at = 90

label-charging = +%percentage%%
label-discharging = -%percentage%%
label-full = %percentage%%

format-charging = <animation-charging>  <label-charging>
format-discharging = <ramp-capacity>  <label-discharging>
format-full = <ramp-capacity>  <label-full>

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

animation-charging-0 = 
animation-charging-1 = 
animation-charging-2 = 
animation-charging-3 = 
animation-charging-4 = 
animation-charging-framerate = 750

[module/title]
type = internal/xwindow
label-maxlen = 70

[module/backlight]
type = internal/backlight
card = nvidia_wmi_ec_backlight
format =  <label>

[module/tray]
type = internal/tray
tray-spacing = 2px

[module/cpu]
type = internal/cpu
format =   <label>

[module/memory]
type = internal/memory
format =   <label>

[module/nvidia-smi]
type = custom/script
format =   <label>
exec = nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits | awk '{ print ""$1"%"}'
interval = 5
