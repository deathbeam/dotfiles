# Load z.sh
[[ -s ~/.awesomedotrc/.lib/z/z.sh ]] &&
  source ~/.awesomedotrc/.lib/z/z.sh

# Load Prezto
[[ -s ~/.awesomedotrc/.lib/zprezto/init.zsh ]] &&
  source ~/.awesomedotrc/.lib/zprezto/init.zsh

# Fix paths
export PATH="/usr/bin:/bin:/usr/sbin:/sbin:$PATH"
export MANPATH="/usr/local/man:$MANPATH"

# Manually set your language environment
export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
export EDITOR="vim"
export VISUAL="$EDITOR"

# Aliases to config files
alias config-zsh="$EDITOR ~/.awesomedotrc/user/zshrc"
alias config-zsh-prezto="$EDITOR ~/.awesomedotrc/user/prezto.zsh"
alias config-vim="$EDITOR ~/.awesomedotrc/user/vimrc"
alias config-vim-plug="$EDITOR ~/.awesomedotrc/user/plug.vim"

# Awesome Star Wars alias
alias star-wars="telnet towel.blinkenlights.nl"

# Awesome Star Wars MOTD
motd[1]=$(echo "$USER, I am your father." | cowsay -f vader-koala)
motd[2]=$(echo "$USER told me enough! $USER told me you killed him!" | cowsay -f luke-koala)
rand=$[$RANDOM % 2 + 1]
echo "\e[33m$motd[$rand]\e[0m"
echo "\nRun \e[33mstar-wars\e[0m to watch some Star Wars!"
